export enum Text{
    DEFAULT_WIDTH = 150,
    DEFAULT_HEIGHT = 20,
}

export enum Picture{
    DEFAULT_WIDTH = 150,
    DEFAULT_HEIGHT = 20,
}

export enum Figure{
    DEFAULT_WIDTH = 75,
    DEFAULT_HEIGHT = 75,
}